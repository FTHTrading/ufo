# x402 + Coinbase CDP for premium GMIIE Ring access
CDP_API_KEY=sk_...
PAY_TO_ADDRESS=your_solana_or_base_usdc_address_here  # e.g. FTH treasury or sovereign x402 payTo
X402_AMOUNT_USDC=0.01
X402_NETWORK=solana   # or base
X402_ASSET=USDC

# Optional: real MCP backend
MCP_SERVER_URL=http://localhost:8000/mcp

# General on-chain / x402 (no specific mint/NFT; Solana/Base RPC for settlement examples if extending legacy-vault)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# Private key or use existing treasury keyring - NEVER commit
